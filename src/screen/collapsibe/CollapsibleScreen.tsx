import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export const CollapsibleScreen = () => {
  return (
    <View style={styles.container}>
      <Text>Collapsible Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});
