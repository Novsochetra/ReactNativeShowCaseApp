import React from 'react';
import {
  ISellRecommendationContainerProps,
  SellRecommendationPresentation,
} from '.';

export const SellRecommendation: React.FC<ISellRecommendationContainerProps> =
  () => {
    return <SellRecommendationPresentation />;
  };
