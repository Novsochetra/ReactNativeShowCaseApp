import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export const CustomSwitchScreen = () => {
  return (
    <View style={styles.container}>
      <Text>CustomSwitch Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});
