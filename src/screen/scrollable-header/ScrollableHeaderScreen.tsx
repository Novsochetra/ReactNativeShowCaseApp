import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

export const ScrollableHeaderScreen = () => {
  return (
    <View style={styles.container}>
      <Text>Scrollable Header</Text>
    </View>
  );
};

const styles = StyleSheet.create({
  container: {},
});
